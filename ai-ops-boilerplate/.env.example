# Copy to .env (git-ignored). This file is for NON-SECRET local config only.
# Real secrets (portal passwords, tokens) belong in the OS keychain via
# core.secretstore — never in this file, never in git.

# EXAMPLE_PORTAL_URL=https://portal.example.com
