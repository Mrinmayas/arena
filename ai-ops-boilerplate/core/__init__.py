"""Vendored reusable core for AI-Ops automations (audit, excel, portals, secretstore, engine).

This directory is copied into each automation repo. Keep it in sync with the boilerplate
via `copier update`; do not fork it per-project without recording why.
"""
